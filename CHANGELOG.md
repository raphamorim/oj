# Changelog

All notable changes to oj are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- The Environment-API dev path (real Vite `DevEnvironment`s driven by the plugin host) now activates for ANY plugin that declares a dev runtime the way Vite reads it — `environments.<name>.dev.createEnvironment` in the raw config, returned from a plugin's `config` hook, or (as Vite also honors) returned from a plugin's `configEnvironment` hook — instead of only for a plugin named `vite-plugin-cloudflare:dev`. Config extraction is the primary detection authority: its verdict (`ssr.runnerBacked`) reaches the plugin host in the spawn payload and gates `buildEnvironments` — a TRUE verdict is authoritative and sufficient, so a declaring plugin whose `config` hook fails inside the host no longer loses the path (the extractor decided independently and `buildEnvironments` re-resolves on fresh instances), while a FALSE or absent verdict falls through to the host's own declaration check (the log names which source decided), so a degraded or stale extraction can never silently disable a worker path the host itself can see declared. An `oj.config.json` that sets any `ssr` key no longer drops the extractor's ssr block either: the two merge per key (oj's keys win; `runnerBacked`, which only extraction produces, is always adopted), and the extraction cache now keys the wrangler configs a Cloudflare `config` hook reads (`wrangler.jsonc`/`.json`/`.toml`, presence included), so fixing a broken wrangler config re-runs detection instead of serving the stale false verdict. When detection said a runtime is declared and no environment came up, the "none came up" warning now prints whatever the failure was.
- The config extractor evaluates `vite.config` ONCE when the app's Vite can resolve it, mirroring Vite's own flow: `loadConfigFromFile` first, its config fed back into `resolveConfig` via `configFile: false`, with runner-backed detection decided inside `resolveConfig`'s own hook run (a sentinel plugin ordered last) — previously the file was evaluated twice (module-level side effects included) and the second, raw-only load could fail alone, silently disabling detection while the resolved workerd `ssr.resolve` sugar was still emitted. If the raw load still fails on a degraded path, the extractor now warns on stderr and withholds the resolved `ssr.resolve` conditions AND the resolved top-level `resolve.conditions`/`externalConditions` (safe defaults over foreign conditions reaching Node consumers undetected). The single-eval flow also gained Vite-exact semantics at three seams: the config file's `build.ssr` no longer rides the inline config into `resolveConfig` (hooks saw `isSsrBuild: true` where real Vite gives false) — it re-enters at Vite's own post-load merge seam, so hooks, the ssr-environment default fill and the emitted build values all still see it; `NODE_ENV` is set from the mode (when the environment leaves it unset) before the config file's module evaluation, as `resolveConfig` does before `loadConfigFromFile`, so configs branching on it at module scope pick the right branch; and the emitted RAW config (`rawResolve`, the "not applied" warnings) is snapshotted before any hook runs, so a plugin mutating the config in place can no longer poison it. When `resolveConfig` throws after the plugin config hooks already ran (a throwing `configResolved`), the sentinel's partial runner-backed verdict is used with a warning naming the error, instead of discarding it and re-running every config hook on the same instances (double side effects). The extractor also exits itself after emitting (a config hook that starts a watcher or interval can no longer keep the subprocess alive), and the Rust side bounds the extraction subprocess (`OJ_EXTRACT_TIMEOUT`, default 60 s) instead of waiting forever — including against a grandchild spawned with inherited stdio, which used to hold the output pipes open past the child's exit (or kill) and wedge boot on the pipe drain: after a short grace the drain threads are detached with the output captured so far.
- The extractor's out-of-band config-hook run (still used when the app's Vite cannot resolve the config) now matches Vite exactly: hook `order` is primary and `enforce` secondary (`getSortedPluginsByHook`), plugins' `configEnvironment` hooks run per environment after the config hooks (with the implicit client/ssr fill), config-hook results merge with the app's own `vite.mergeConfig` when available (the fallback merge gained Vite's `true`-wins rule for `ssr`/`resolve` `noExternal`/`external`), and `configEnv.isSsrBuild` is `false` for serve AND build, as in Vite, which computes it from the inline config before the config file loads and never recomputes it.

### Fixed
- `oj build` of an `ssr.target: "webworker"` bundle under a runner-backed config no longer swaps in Node resolution conditions: a worker-target ssr bundle executes in the worker runtime itself, so the environment's own conditions stand.
- The Start prewarm's serve-info wait is anchored to the plugin host's own init deadline (spawn instant + `OJ_PLUGIN_INIT_TIMEOUT`) instead of starting a second full period of its own once the build finishes — and it is additionally bounded at the RPC-timeout scale (`OJ_PLUGIN_TIMEOUT`): a host that never pushes its serve info can no longer hold the Start fallback runner cold for the whole init deadline (a late activation still supersedes the prewarm).
- `NODE_ENV` during config extraction now derives from the COMMAND, as in Vite (`serve` → `development`, `build` → `production`): `oj build --mode staging` evaluates `vite.config` under `NODE_ENV=production`/`isProduction: true` where it previously saw the mode's name, and `oj dev --mode staging` stays `development`. The extractor's pre-load value is also unset again right before `resolveConfig` runs, so Vite's own `VITE_USER_NODE_ENV` handling (a `.env` file's `NODE_ENV=development` under build) is no longer disabled by the pre-set; a `NODE_ENV` already set in the environment is never overridden.
- The extraction cache now stamps the wrangler configs the config evaluation ACTUALLY reads — a `cloudflare({ configPath })` relocation and `auxiliaryWorkers` configs included, existence probes of missing files too — so editing, creating or deleting a non-root wrangler config invalidates the cached verdict like a root one.
- When `resolveConfig` throws after plugin config hooks began running, a runner environment declared by the config FILE itself no longer false-negatives the partial runner-backed verdict (a hook throwing before the detection sentinel's post-ordered sniff left it false even though the raw declaration was in plain sight).
- The late-activation catch-up resync is acknowledged when it is ENQUEUED on the host's serialized invalidate queue, and duplicates coalesce into the pending one: a resync stuck behind a slow hot-update no longer times out the caller's bounded POST, stacks a second full-reload, or logs a false "edits may be stale" warning.
- A lazily spawned plugin host whose init already outlived one call's full window now fails later pre-init calls fast (the failure is latched) instead of freezing each consumer for a full `OJ_PLUGIN_TIMEOUT` window apiece; a late init completion clears the fail-fast and calls flow normally again.
- An `oj.config.json` `ssr.resolve` block now merges per-key with the extractor's (oj's sub-keys win, missing ones fill in), so an oj-side `externalConditions` no longer drops the extracted workerd `conditions`; a non-object oj-side `ssr` (or `ssr.resolve`) value warns and adopts the extractor's block instead of silently dropping `runnerBacked`.
- The plugin host's config-hook merge now matches Vite's `mergeConfigRecursively` where it did not: a hook returning `key: null` no longer clobbers a set value, and `true` on either side of ssr/resolve `noExternal`/`external` wins over lists.
- A Vite-shaped `resolve.mainFields` list (Vite's defaults omit `"main"` because its resolvePackageEntry always falls back to `pkg.main` afterwards) made a package whose ONLY entry is `main` unresolvable once adopted from the config — e.g. a linked workspace package with a TS `main` and no index file. oj's resolver now appends the same `main` fallback (last, never outranking the configured fields), mirroring Vite's `entryPoint ||= data.main`.
- A module that both reads `import.meta.hot` itself AND is a React refresh boundary (a TanStack route file: the router plugin injects its own `import.meta.hot` handler) was served with the hot-context banner twice — the prepended banner plus the refresh glue's own import — and two import bindings named `__oj_createHotContext` in one module scope are a SyntaxError that killed the module in the browser. The refresh glue now reuses a predefined hot context instead of re-declaring it, as Vite's refresh footer reuses the import-analysis banner.
- The Node SSR paths could resolve a package's `browser` conditional export and execute DOM code server-side (`ReferenceError: document is not defined` at module scope, e.g. `decode-named-character-reference`'s `index.dom.js`, failing every SSR render). Two halves. First, on an app whose ssr environment carries a workerd condition set (the Cloudflare plugin's `workerd, worker, module, browser`), `resolve.conditions` never saw it: the resolved ssr conditions travel on the extractor's `ssr.resolve` sugar, and the lookup fell through to the top-level `resolve.conditions` — which a resolved Vite config fills with the *client* defaults, a browser-bearing list that steered every noExternal/linked resolution into browser builds. `resolve.conditions` now falls back environment → `ssr.resolve` sugar → top level, exactly like `externalConditions` already did. Second, conditions never cross runtimes (Vite's model: an environment's conditions steer resolution only for code that executes in that environment's own runtime). The config extractor now marks the ssr environment "runner-backed" (`ssr.runnerBacked`) from Vite's own declaration mechanism — the raw config, or a plugin's `config` hook return merged the way Vite's `resolveConfig` merges them (before Vite default-fills every environment's factory), declares `environments.<name>.dev.createEnvironment`; the same declaration gates the plugin host's `buildEnvironments` — and every Node-executing consumer (the Start loader's env plumbing, the plain unbundled SSR resolver) then takes Vite's Node server semantics instead of the foreign runtime's list: `DEFAULT_SERVER_CONDITIONS` (`module`, `node`, dev/prod) plus the user's RAW top-level `resolve.conditions` (published as `rawResolve`; the resolved top-level list is the client environment's and never crosses), and `DEFAULT_EXTERNAL_CONDITIONS` (`node`, `module-sync`) unless the raw top-level `resolve.externalConditions` replaces them, as a user list does in Vite. A non-runner-backed environment's list passes verbatim, no stripping: an explicit user `browser` (happy-dom-style Node SSR) stays honored, as Vite honors user conditions. The generic SSR production bundle (`oj build`'s Node-target server output) applies the same source selection instead of inheriting a workerd-shaped list verbatim, and the stale "ssr.resolve is not applied" config warning now names only the genuinely-inert subkeys (`conditions`/`externalConditions` are applied).
- On a slow boot (a large plugin fleet, a Miniflare boot inside `configureServer`) every boot-time plugin RPC burned its own 20 s timeout and permanently snapshotted wrong defaults — config()-hook defines and env mutations were dropped, hook gates defaulted, and on Cloudflare apps `getServeInfo` failed so the worker path silently never activated (no `plugin middleware: forwarding unmatched requests` line; every document degraded to the Node SSR runner for the whole session). The host answers RPCs only after its top-level init, and its init-complete moment is now observable — it pushes `{ ojServeInfo }` the instant init finishes — so RPC sends are gated on an "initialized" state derived from that push or the first reply: before init, a call waits (bounded by `OJ_PLUGIN_INIT_TIMEOUT`, default 300 s, with a "plugin host still initializing (Ns)…" progress line every 30 s) instead of racing its per-call timeout against the boot; after init the 20 s per-call timeout applies unchanged, and fast boots behave byte-identically. Should a host still outlive the init deadline, activation is now a proper transition rather than a flag flip: the middleware state is one atomically packed snapshot (readers can never see the port without the serve mode), the flip synchronously re-arms the Start fallback runner (`runner_dirty`) before it becomes visible — closing the window where an edit racing the flip took neither reload path — and one catch-up resync (`{"resync":true}` on `/__oj_invalidate`) invalidates every runner-backed environment's whole module graph and full-reloads it, so edits made during the degraded window are never lost — "resynced" is logged only when the middleware actually acknowledged the resync (a transient failure is retried with backoff; a resync that never lands warns that degraded-window edits may be stale instead of claiming success). The warning also stopped guessing from config text (the raw `@cloudflare/vite-plugin` substring search false-positived on commented-out imports): oj warns only when the host never finished initializing within the deadline, and the host itself — which knows whether a runner-declaring plugin is active — warns when one is present but no environment came up. The init gate's reach is scoped per spawn: only the boot-time host (whose snapshot RPCs boot correctness depends on) waits out the long deadline, while a lazily spawned host (the SSR environment host, created on the first SSR request) bounds its init wait by the ordinary 20 s per-call timeout — anchored to EACH call's own start (a spawn-anchored bound gave calls arriving after spawn + 20 s during a pending init a zero-length window: instant failure), so a wedged init degrades like a slow hook instead of freezing the watcher thread's HMR dispatch and browser-facing SSR transforms for minutes. The host also signals init-complete explicitly in BOTH modes (`{ ojInit: true }`, once its RPC listener is up): build mode has no serve-info push, and previously a hanging first hook there waited out the whole init deadline blamed on initialization. The Start prewarm's serve-info wait is bounded by the same `OJ_PLUGIN_INIT_TIMEOUT` knob instead of a second hardcoded number, an activation landing between server build and the Start hook's registration is caught up inline (the fallback runner is still marked dirty), and a plugin host that dies before ever initializing is reported ("plugin host exited before initializing") and released instead of being pinned by its own watcher task forever.
- The plugin host's control plane shared a raw stdout line protocol with plugin code: a plugin's unterminated partial write could splice the one-shot `{ ojServeInfo }` push (parse fails, silently dropped, never retried), and any plugin output — including attacker-controlled content a plugin echoes — that happened to spell a protocol line could forge a push or an RPC reply. Every oj protocol line (RPC replies, ctx-RPC requests, and the `ojServeInfo`/`ojServer`/`ojWs` pushes) is now framed: a leading newline terminates any dangling partial line, and a per-session random token (`OJ_CONTROL_TOKEN`, minted at spawn) prefixes the line — the reader ignores unframed lines entirely, so plugin prints can never be parsed as protocol (forging requires reading the host's env: the same trust domain as running plugin code). The serve-info push is additionally re-sent once a second until oj ACKs it, so even a spliced copy heals.

## [0.1.18] - 2026-09-05

### Fixed
- A TypeScript `vite.config` that relatively imports a sibling workspace package's source (a monorepo shape) failed to load ("failed to load config", `Cannot find package '...' imported from .../oj-vite-config-....tmp.mjs`) when it was bundled by oj's own esbuild fallback: `packages: "external"` kept bare specifiers bare, so the bundle -- imported from the cache dir -- re-resolved the inlined sibling source's dependencies from the cache dir instead of from the sibling's `node_modules`. Both config bundlers (the extractor and the plugin host) now mirror Vite's `externalize-deps` plugin: a bare import is resolved from its importer at bundle time and externalized as the resolved absolute path (a `file://` URL; the bundle is ESM), while first-party resolutions -- TS/TSX sources and anything outside `node_modules`, e.g. a tsconfig-paths alias, which also stays a watched config dependency -- and `.json` are inlined as before. Bare resolution follows Node's rules (`conditions: ["node"]`, `mainFields: ["main"]`) instead of esbuild's bundler defaults, which preferred a package's `"module"` exports target over the `"import"`/`"require"` one Node loads; node/bun builtins and `npm:` specifiers stay bare, other scheme-shaped ids (`data:`, ...) are left to esbuild, and an unresolvable specifier is still left bare but now gets a one-line stderr warning naming the specifier and its importer. Both bundlers also gained Vite's `inject-file-scope-variables`: `__dirname`, `__filename` and `import.meta.url` (plus `import.meta.dirname`/`.filename`) now carry each inlined file's original location instead of the config file's (or, for `import.meta.url`, the cache-dir tmp bundle's).

## [0.1.17] - 2026-09-05

### Changed
- Cloudflare dev (`@cloudflare/vite-plugin`): on a source edit the worker environments no longer get a blanket `full-reload` that made the module runner re-walk the whole server graph (the next document after an edit took several times what `vite dev` takes on a large TanStack Start app). oj now mirrors Vite's HMR propagation over each environment's module graph (plugin `hotUpdate` hooks included) and sends targeted `{type:"update"}` payloads to the nearest accept boundary (the plugin's worker entry self-accepts), falling back to `full-reload` where Vite does (a propagation dead end, a graph without the walk API) and sending Vite's `{type:"error"}` payload when a `hotUpdate` hook throws. Changes travel with Vite's watcher event types: a delete goes through `moduleGraph.onFileDelete`, a created file retries the modules whose imports previously failed to resolve, `pluginContainer.watchChange` runs with the matching event, legacy `handleHotUpdate` plugins are dispatched for the client environment, and a hook's `read()` retries a truncated write as Vite's `readModifiedFile` does.
- Cloudflare dev: the Node SSR runner becomes a cold fallback when the worker environments serve the documents — the boot prewarm render is skipped and the per-edit runner reload is replaced by a lazy reload right before the runner's next actual use. Server environments get `dev.preTransformRequests` enabled unless the config sets it, so the module runner's serial import walk is pipelined by import-analysis pre-warming. Environments are created and initialized in parallel, as Vite's `createServer` does, and `OJ_BOOT_PHASES` marks the second `resolveConfig` and each environment's creation and init. Apps without the plugin are untouched.

### Fixed

- `resolve.externalConditions` set in a `vite.config.*` was silently dropped: the extractor only carried `extensions`/`mainFields`/`conditions`, so the option worked from `oj.config.json` alone while externalized SSR deps fell back to the default conditions. It is now extracted top-level, through Vite's `ssr.resolve` sugar and the `environments.ssr.resolve` spelling (the environment wins), and looked up with the same precedence.
- The SSR loader's persistent caches were keyed without `resolve.externalConditions` and `ssr.noExternal`/`external`, so editing either and restarting replayed the previous run's resolutions; `oj.config.json` was also missing from the cache's base inputs. All three now invalidate the cache.
- `browser` was stripped from the user's ssr `resolve.conditions` for every app; it is now dropped only from a workerd-shaped list (one that also names `workerd`, the Cloudflare plugin's set, which must not pick browser builds in the Node loader) and an explicit user `browser` condition is honored, as Vite honors user conditions.
- A metadata-only file event on a never-seen path was dropped, so the first `touch` of a file per session did nothing (a second worked). With no recorded baseline, a fresh modification time now counts as a change; an old one is still the access-time noise the filter exists to ignore.
- Config evaluation raced itself at boot: the route tree, the server-fn resolver, the config extractor and the plugin host all bundle `vite.config.ts` concurrently, and the extractor script plus the bundled config shared fixed filenames, so one process could truncate a file another was importing (`could not extract vite.config values`, `failed to load config`, an extractor that "wrote nothing at all"). The script now lands by atomic rename, every bundled config gets a per-process temp name that is removed after import, and the extractor writes its JSON to a per-call result file instead of stdout, which plugin code (a route generator's warnings) could pollute.
- Cloudflare dev: worker-served documents did not get the live-reload client injected, so a tab on a plugin-served app never reloaded on edits. The plugin middleware's HTML responses now get the same tail injection as runner-served documents (responses with a `content-encoding` are left untouched, on every injection path).
- A document requested milliseconds after a save could be rendered from stale worker modules: the watcher now invalidates the worker environments on the first raw file event, before its 50 ms settle window and the rebuild, and again with the settled batch. The invalidation endpoint also answers only after the module graphs are updated.
- TanStack Start dev: a second edit landing while the first edit's client rebundle was in flight queued behind it (the watcher thread sat inside the rebundle), so interim documents served the previous version and fresh content appeared only seconds after the second edit. The watcher thread now only receives, classifies, settles and hands batches off, the way Vite's watcher callbacks run per event; worker invalidation fires promptly per batch on the async runtime, and the client rebundle plus route-tree/server-fn regeneration run in a coalescing worker (batches landing mid-run merge into at most one pending rerun). The run keeps the inline loop's ordering: a regenerated route tree or server-fn resolver gets its own worker invalidate after regen (the watcher deliberately never forwards those files), the lazy runner's dirty flag is re-armed post-regen so a fallback request that consumed it mid-run cannot hold stale generated files, and the non-lazy runner reloads only after regen, all before the browser reload, which still fires once per completed rebundle, after that batch's settled invalidation.
- Cloudflare dev: the early (pre-settle) and settled invalidation sends for one write usually carry the same change, so the plugin host replayed the watcher event and the whole `hotUpdate` pipeline (hooks, targeted updates, the runner's chain re-fetch) twice per edit. The host now skips a change whose type and file content hash match the last processed one, mirroring Vite's single chokidar event per FS change: identical consecutive content is the early/settled pair, while a write landing inside the settle window, a revert, and same-mtime distinct writes all still process (a read error also always processes). A "create" for a file a runner environment's module graph already knows is an atomic save's recreate half and is handled as an update, closing the update-then-create double pass and the phantom watcher "add".
- The dev watchers treated attribute-only file events as changes. On Linux the first read of a file after it was written updates its access time, which inotify reports as an attribute change, so a rebuild that reads every source file (the TanStack Start client bundle does) looked like an edit of every source file and triggered another rebuild until the access times settled, with the HMR gate filling up with phantom changes. Both watchers now apply chokidar's rule: a change is a data change or a moved modification time; an attribute event with an unchanged mtime (or on a file never seen before) is ignored, and a `touch` still counts.

## [0.1.16] - 2026-09-03

### Added
- TanStack Start: an app's own server entry (`tanstackStart({ server: { entry } })`, the shape an SSR error wrapper takes) is honored in dev and in the prod server bundle, as Vite imports it: the configured module is the handler and `@tanstack/react-start/server-entry` inside it is oj's handler, so a wrapper composes. Previously oj always ran its own entry and the wrapper never executed.

### Fixed
- TanStack Start dev ignored the editor-driven HMR gate: every write under `src/` rebuilt the client bundle and reloaded the page at once, so a preview reloaded over and over while files were being written. The rebuild still happens immediately, but the page reload now waits for `POST /__hmr_flush` (or the 240 second hold cap), as plain-mode updates already did and as the editor's Vite gate plugin holds a bundled dev server's full reload; `/__hmr_gate` reports `heldReload` and `startedAt`, and the flush response says whether a reload was released.
- `vite.config` "not applied" warnings were emitted for Vite's own resolved defaults (`esbuild.jsxDev/charset/legalComments`, `worker`, `ssr.resolve`, `server.cors.origin`, `optimizeDeps.esbuildOptions`, `build.terserOptions`, the `@vite/env` and `@vite/client` aliases) on every config, and repeated on every config load (three times at Start startup, again after each rebuild). They now describe only options the config file itself sets, and each line prints once per dev session.

## [0.1.15] - 2026-09-03

### Fixed
- TanStack Start dev behind a proxy that sets `x-forwarded-host` (a preview or a tunnel) answered every request with `500 Failed to parse URL from http://proxy-host, localhost:port/`: oj forwarded the browser's `Host` to the SSR runner as a second `x-forwarded-host`, and Node joined the two. The original `Host` now travels in a private header and is restored as `Host` (first value only, as Node does for duplicate `Host` headers), and a proxy's `x-forwarded-host` reaches the app untouched, as under Vite. Plugin middlewares (`configureServer`) see the browser's `Host` in `req.headers.host` for the same reason.

## [0.1.14] - 2026-09-03

A Vite-parity release: the fifth oj-versus-Vite audit round closed its last P0 (production builds for Cloudflare), every remaining P1 that did not need a redesign, and most of the P2 list across the dev server and HMR, the resolver and dependency optimizer, plugins, the production build, CSS and assets, and SSR with TanStack Start. Every change below ships with a unit or end-to-end test and was matched against rolldown-vite 8.3.0-beta.0.

### Added
- Cloudflare: when `vite.config` uses `@cloudflare/vite-plugin`, `oj build` of a TanStack Start app drives the plugin's own hooks (`config`, `configEnvironment`, `configResolved`, `generateBundle`, `writeBundle`) and emits Vite's layout: `dist/client`, a Worker bundle per declared environment built for workerd (platform neutral, `cloudflare:*` and native `node:*` left external, no Node http wrapper, no `createRequire` banner), the plugin-written `wrangler.json` and `.wrangler/deploy/config.json`. The output runs under `wrangler dev` unchanged. The Node server build for apps without the plugin is unchanged.
- `build.lib` from `vite.config` is honored with Vite's entry forms (string, array, object), default formats (`es` and `umd` for one entry, `es` and `cjs` for several), package-name and `type`-driven file names, and `cssFileName`.
- HTML asset attributes are rewritten to hashed URLs in the build like Vite's asset pipeline: `img src`/`srcset`, `link` `icon`/`manifest`/`stylesheet` hrefs, `video`/`audio`/`source`/`track`/`poster`, `use`/`image` hrefs and `meta` image properties, with `publicDir` references prefixed by `base`, small files inlined and `vite-ignore` respected. Inline `<script type="module">` bodies are externalized regardless of attribute quoting.
- CLI: `oj build` accepts `--base`, `--outDir`, `--assetsDir`, `--assetsInlineLimit`, `--target`, `--sourcemap [output]`, `--minify [minifier]`, `--manifest [name]`, `--ssrManifest [name]`, `--emptyOutDir`, `-m/--mode`, `-c/--config` and `--app`; `--watch` is rejected with a clear message. The TanStack Start build honors `--out`/`build.outDir`, `base`, `build.sourcemap` and `build.minify`.
- `build.assetsDir` places hashed chunks, stylesheets and assets (an empty value puts them at the outDir root); `build.cssTarget` and `build.cssMinify` drive CSS lowering and minification (`cssMinify` follows `build.minify` when unset); `build.manifest` accepts a file name and the manifest gains asset rows, CSS rows and per-chunk `assets`; `build.reportCompressedSize` and `build.chunkSizeWarningLimit` are honored, and the build report prints every file with a gzip column and Vite's large-chunk warning. `rollupOptions.output` `format`, `banner`, `footer`, `intro`, `outro`, `globals`, `paths`, `name`, `extend`, `inlineDynamicImports` and `treeshake` are forwarded to rolldown.
- `?worker&inline` workers are bundled with the app's `resolve`, `define`, `target` and `minify` settings and oj's asset and CSS handling; worker-only chunks no longer receive the page preload and CSS injection helpers.
- `oj preview` honors `preview.cors`, `preview.allowedHosts`, `preview.headers`, `preview.strictPort`, `preview.open` and `appType: "mpa"`, inheriting from `server.*` as Vite does.
- CSS: `resolve.alias` and root-absolute specifiers resolve inside `@import`, `url()` and Sass `@use` in dev and build (a `public/` file wins over a same-named source file); CSS modules export every legal class name as a named export next to the default map; `css.modules` `localsConvention`, `generateScopedName`, `scopeBehaviour`, `globalModulePaths` and `composes ... from "./other.module.css"` are honored; bare Less imports resolve through `node_modules`; `html.cspNonce` stamps every served and built page; `import.meta.glob` `base` and `exhaustive`; the `?no-inline` asset query.
- Dev server: Vite's html fallback (`/page` serves `page.html`, `/dir/` serves `dir/index.html`) with `appType` `spa`, `mpa` and `custom`; `server.hmr` `path`, `host`, `clientPort`, `protocol` and `overlay: false`; the error overlay is a `vite-error-overlay` custom element; `server.watch.ignored`, nested `node_modules` and `.git` ignores, a restart when a file the config imports changes, and `server.open` (honoring `BROWSER`, `none` disables it); `server.fs.strict: false`.
- HMR: `import.meta.hot.prune` frames when an import disappears (a removed stylesheet import removes its styles), `import.meta.hot.invalidate` with `firstInvalidatedBy`, the once-per-update guard and Vite's "circular import invalidate" reload, `isWithinCircularImport` on updates (a failed re-import inside a cycle reloads instead of showing an overlay), `.json` imports stamped with `?t=` so data edits reach the boundary, and `import.meta.glob` importers updating when a matching file is created or deleted. A startup error frame is held until the client connects, and an update arriving while the overlay is up reloads the page.
- Resolver: `.js`-suffixed imports remap to `.ts`, `.tsx`, `.mts` and `.cts` for aliased and tsconfig-paths imports too; `require()` inside a served CommonJS dependency resolves with the `require` condition; `node:` ids are builtins and the browser stub warns on access with Vite's "externalized for browser compatibility" message; a missing optional peer dependency resolves to Vite's lazy error stub; the SSR resolver ignores the `browser` field.
- Dependency optimizer: `optimizeDeps.include` globs, `optimizeDeps.needsInterop`, `optimizeDeps.entries` globs and `resolve.dedupe` are honored; optimized dependency URLs carry `?v=<hash>` and are served with `max-age=31536000,immutable` plus ETag and 304, and the hash covers every Vite lockfile format, the patches directory and the mode. Plugins run on `optimizeDeps.exclude` packages, as Vite's un-prebundled dependencies do.
- Plugins: `this.resolve` and `this.load` consult sibling plugins before the resolver and the disk (per-plugin contexts, `skipSelf`); `hotUpdate` and `watchChange` receive `create` and `delete`; plugin `config()` `define` entries reach dev and build; per-hook `order` on `transform`, `resolveId` and `load`; `applyToEnvironment` return values replace the plugin; `transformIndexHtml` receives the real per-page context (`path`, `filename`, `server`, `bundle`, `chunk`); `server.environments` always exposes `client` and `ssr`, `server.httpServer` is addressable and emits `listening`, and `server.ws.on("connection")` fires; `ConfigEnv` carries `isSsrBuild` and `isPreview`, `apply(config)` sees the merged config, `this.meta.viteVersion` is set, and config discovery follows Vite's `js, mjs, ts, cjs, mts, cts` order. `OJ_PLUGIN_TIMEOUT` sets the per-hook RPC timeout.
- SSR and TanStack Start: `oj dev --mode` selects `.env.<mode>`, `import.meta.env.MODE` and the mode plugin hooks see; the dev client bundle applies the config `define` like the SSR loader; `envPrefix` and `environments.{client,ssr}.define` reach dev and prod bundles; `ssr.external` keeps listed dependencies as bare imports of the Start server bundle; `resolve.conditions` and JSON named exports in the Start loader; `noExternal` dependencies run through the plugin pipeline; `import.meta.glob` `base`, `as` and `caseSensitive`; the SSR manifest lists the CSS of dynamic imports and the assets each module imports; SSR stack traces map back to original `file:line:col`.

### Changed
- The build manifest is written only when `build.manifest` is set (Vite's default). Enable it explicitly to keep `.vite/manifest.json`.
- `process.env`, `global.process.env` and `globalThis.process.env` are defined to `{}` in browser builds (SSR keeps `process.env`); `import.meta.hot` is defined to `undefined` in builds; `minify: false` no longer minifies CSS and still tree-shakes JS.
- Browser WebSocket clients of the dev server must send the per-process `?token=` Vite 6 introduced; clients without an `Origin` header are unaffected and `legacy.skipWebSocketTokenCheck` disables the check. Both dev clients reload after a server restart instead of reconnecting with a stale token.
- CSS modules are no longer self-accepting: an edit propagates to the importer so new class names apply, and an importer without an accepting boundary above it full-reloads, exactly as in Vite.
- The default CSS lowering targets moved from `chrome100`/`safari14` to Vite's baseline browsers (`chrome111`, `edge111`, `firefox114`, `safari16.4`), and follow `build.cssTarget` or `build.target` when set.
- `publicDir` files are served verbatim in dev (a public `.html` no longer receives the dev client) and `publicDir: false` disables the directory; `?url` on a public file yields its public URL.
- The default `server.fs.allow` is Vite's workspace root (`pnpm-workspace.yaml`, `lerna.json`, a `package.json` with `workspaces`, else the nearest `package.json`); a `.git` directory is no longer a workspace marker and the allow list is not widened to the farthest `package.json`. A user `fs.allow` replaces the default.
- Default `resolve.extensions` follow Vite (`.mjs, .js, .mts, .ts, .jsx, .tsx, .json`), so an existing `.js` wins over a sibling `.ts` and `.cjs` is not probed for extensionless imports.
- An unresolvable relative or bare import in app source fails the transform with Vite's `Failed to resolve import "x" from "src/a.tsx". Does the file exist?` (500 plus overlay) instead of shipping the specifier to the browser; creating the missing file recovers without a restart.
- `.html` edits reload only pages that show that file; HMR frames use Vite's shapes (`path` and `triggeredBy` on full reloads, `acceptedPath`/`timestamp`/`firstInvalidatedBy` on updates).
- Errors thrown from `buildStart`, `renderStart`, `generateBundle`, `writeBundle`, `closeBundle` and `buildEnd` fail the build (non-zero exit, `[plugin:name]` message) and dev startup; `buildEnd(error)` and `closeBundle` run on failed builds and when `oj dev` exits; a throwing `transformIndexHtml` fails the request and the build instead of serving raw HTML.
- SSR and client-entry builds pass the resolved `--mode` to the plugin hosts (previously always `production`). TanStack Start dev no longer forces `NODE_ENV=development`: the shell's value wins and `.env` may only pick `development`, as in Vite.
- The generic `oj dev --ssr` runner speaks loopback HTTP instead of stdin JSON: requests run concurrently (an app fetching its own routes during render no longer deadlocks) and action bodies arrive as bytes. TanStack Start request bodies are streamed (no 100 MB buffer), dotted non-GET paths reach the app, and the generated `dist/server.mjs` streams responses and preserves every `set-cookie` header.
- lightningcss parses stylesheets with error recovery, so one legacy hack no longer fails the whole file (star-hack declarations are dropped with a warning); `@import` inlining runs before PostCSS, as postcss-import does; `?inline` stylesheets run the full CSS pipeline in dev.
- A failed build no longer leaves a partial `dist/`, an `outDir` outside the project root is never emptied, and `build.outDir` equal to the project root is refused.
- Unsupported build options (`write: false`, `watch`, `license`, `commonjsOptions`) and function-valued config values that cannot cross the config bridge (`server.proxy` `configure`/`bypass`/`rewrite`, `assetsInlineLimit`, `css.modules` callbacks) are reported with a warning instead of being ignored silently.

### Fixed
- Config `define` was applied to the SSR render but not to the TanStack Start dev client bundle, so a define used in a component rendered on the server and threw a `ReferenceError` on hydration.
- A `null` `environments` payload sent to the plugin host erased Vite's resolved environments, so `environments.ssr.define` never reached the SSR loader.
- `server.proxy` contexts starting with `^` were never matched (they were compared as literal prefixes); they are now regular expressions tested against path and query, in the middleware, the WebSocket tunnel and the SSR classifier.
- A page opened on a startup compile error never recovered: the error frame was broadcast before the client connected, and the fix's update was ignored because the module had never loaded.
- The resolver cached misses forever, so a file created after a failed import stayed unresolvable until restart.
- The client entry of an SSR app (`oj build --ssr`) ignored `base` and `build.assetsDir` in its injected script and stylesheet URLs.
- The extraction cache for `vite.config` was written twice and was not keyed on `VITE_*` or `NODE_ENV`, so an environment change could serve values computed under the old one.

## [0.1.13] - 2026-09-03

### Added
- `server.proxy` now reaches `wss://` targets (plain `https://` targets already worked): a proxied WebSocket upgrade to a TLS target is dialed over TLS with the system trust store, the same store oj's HTTP proxying verifies against, and tunneled as before. `secure: false` on an entry (http-proxy's option, which Vite passes through) accepts a self-signed development backend for both the WebSocket tunnel and plain requests; by default an unverifiable certificate is refused with 502. No new crypto stack: the TLS client is the rustls provider reqwest already links. On a proxied upgrade the browser's `Host` is kept unless `changeOrigin` asks for the target's (http-proxy's rule; it used to be replaced always), and `rewriteWsOrigin: true` swaps the `Origin` for the target's origin, as in Vite.

## [0.1.12] - 2026-09-03

A Vite-parity release: four oj-versus-Vite audit rounds closed every P0 and P1 gap found across the resolver, dev server and HMR, plugin API, production build, CSS and assets, and SSR, and 47 contributor pull requests were landed. Thanks to @williamhogman and @mikn.

### Added
- `ssr.noExternal`, `ssr.external` and `ssr.target` are honored (from `vite.config` or `oj.config`): in `oj build --ssr` and the server-function bundle, `external` names stay external, `noExternal` matches (package names, `@scope/*` globs, RegExps, or `true`) are bundled and transformed, and `target: "webworker"` builds for the browser platform; in TanStack Start dev the SSR loader transforms `noExternal` dependencies itself (define, `import.meta.env`, `import.meta.glob`) instead of handing them to Node. Previously every `node_modules` module was external.
- TanStack Start dev: the SSR loader inlines a source map into every transformed module and Node runs with `--enable-source-maps`, so server stack traces point at the original `.tsx` positions (Vite's `ssrFixStacktrace`).
- `server.moduleGraph.getModuleById`/`getModulesByFile`/`invalidateModule` are wired to oj's module cache and HMR: invalidating a module drops its compiled output (the next request re-runs plugin transforms) and, for a file in the app, propagates an update as a file change would. `server.restart()` restarts the dev server. Both used to be silent no-ops on a stub, so the "invalidate a virtual module then reload" plugin idiom did nothing.
- Every plugin hook now accepts Vite's object form `{ order, filter, handler }` (config, configResolved, configEnvironment, configureServer, hotUpdate/handleHotUpdate, buildStart, moduleParsed, watchChange, buildEnd, renderStart, closeBundle, generateBundle, renderChunk, writeBundle), and a hook's plugins run in `order` (pre, normal, post). Previously only transform/resolveId/load unwrapped the handler and other object-form hooks were silently skipped.
- `configEnvironment(name, options, env)` runs for every environment after the config hooks and its result is merged into that environment's options.
- `hotUpdate` (Environment API) is dispatched, taking precedence over `handleHotUpdate`, and both receive `server` in their context (`server.ws.send`, `server.middlewares`).
- `oj build --config <file>` and `oj preview --config <file>` use that config file (Vite's `--config`). A `mode` named by the config file is the default mode when the CLI gives none (Vite: `inlineConfig.mode || config.mode || "production"`), and the config is reloaded under it.
- More of `vite.config` reaches oj: `resolve.extensions`, `resolve.mainFields`, `resolve.conditions`, `resolve.preserveSymlinks`, `server.strictPort`, `server.open`, `server.cors`, `css.preprocessorOptions.<lang>.additionalData` (string form), `envPrefix` and `envDir`. The build now forwards `resolve.*` to rolldown (a module that resolves in dev also resolves in the build) and reads `.env` files from `envDir` with the configured `envPrefix` instead of hardcoding the root and `VITE_`.
- SSR builds honor Vite's `ssr` options: `build.ssr: true` takes the entry from `rollupOptions.input`, `ssr.noExternal` (a list or `true`) bundles those dependencies into the server bundle, `ssr.external` keeps its entries external even then, and `ssr.target: "webworker"` bundles every dependency for a browser-like platform. Externalized dependencies are now imported by their bare specifier (`from "react"`) instead of an absolute `node_modules` path. `build.ssrManifest` (`true` or a file name) writes Vite's `ssr-manifest.json` from the client build: each source module maps to its chunk and stylesheet URLs.
- Chunking options: `output.advancedChunks` (rolldown's native form, groups with regex or string tests, priorities and size limits) is applied; a `manualChunks` object value that is a `./` or absolute path now matches that source module (Rollup semantics) while bare names keep matching packages under `node_modules`; and a function-valued `manualChunks`, `entryFileNames`/`chunkFileNames`/`assetFileNames` or `external` is reported with a clear warning instead of being silently dropped on the way through the config's JSON boundary (both `oj.config` and `vite.config`).
- `base: "./"` (and `""`) is now a relative base as in Vite: HTML script, modulepreload and stylesheet URLs are emitted relative to each page (`./assets/…` from the root page, `../assets/…` from a nested page), CSS `url()` references become sibling paths, `import.meta.env.BASE_URL` is `./`, and dynamic-import preloads resolve against the importing chunk. It used to collapse to `/`.
- Dynamic imports in the build now go through Vite's `__vitePreload`: a lazy chunk's statically imported chunks and their stylesheets are preloaded in parallel with it, its CSS is applied before it executes (no flash of unstyled content), and a failed preload dispatches `vite:preloadError` on `window`. Page entries get Vite's modulepreload polyfill unless `build.modulePreload` is `false` or `{ polyfill: false }`.
- `build.sourcemap` accepts Vite's `"inline"` and `"hidden"` (hidden writes the `.map` without a `sourceMappingURL` comment), `build.minify` accepts a minifier name (`"oxc"`, `"esbuild"`, `"terser"`, all meaning on), and `build.terserOptions` is accepted with a warning instead of failing config load. From `vite.config` these string forms were previously downgraded or dropped.
- `server.proxy` entries with `ws: true` now tunnel WebSocket connections to the target (path, query, cookies and subprotocols forwarded; the upstream's selected subprotocol relayed), and proxied HTTP responses stream through chunk by chunk, so server-sent events and long-polling backends work behind the proxy. Large or chunked request bodies stream too instead of being buffered. `wss://` targets are not supported yet.
- `import.meta.hot` works in any module, not only React components. A module that references it gets a hot context; `import.meta.hot.accept()` / `accept(cb)` makes it self-accepting, `accept('./dep', cb)` and `accept([deps], cb)` make it the update boundary for those dependencies and hand the callback the new dependency module (or the deps-aligned array), `dispose` runs before replacement, and update frames carry the `acceptedPath`. Previously only React Fast Refresh registrations formed boundaries and `accept(deps, cb)` called back with the wrong module.
- `oj dev --mode <mode>` selects `.env.<mode>`, sets `import.meta.env.MODE`, and is the mode a function config and plugin `config` hooks receive, like `vite dev --mode`. Previously the dev server was always `development`.
- `server.cors` is applied. Unset means Vite's default (only localhost origins are reflected), `true` reflects any origin, `false` sends no CORS headers, and an object carries `origin` (string or list), `methods`, `allowedHeaders`, `credentials` and `maxAge`; preflight requests are answered. Read from `vite.config` too.
- `server.allowedHosts` is enforced (Vite's host check against DNS rebinding): a request whose `Host` is not localhost, an IP literal, the configured `server.host`, or a listed host (a leading `.` allows subdomains) is refused with 403, and a WebSocket upgrade whose `Origin` fails the same check is refused. `allowedHosts: true` disables the check. Read from `vite.config` too.
- `css.devSourcemap: true` now appends an inline source map to dev-served CSS (after Sass/PostCSS, mapping to the served stylesheet url with the source embedded), as in Vite; the option was ignored.
- HTML pages as build entries now handle `<link rel="stylesheet" href>` to a local file (relative or root-absolute, Sass/Less/PostCSS/Tailwind included) by compiling it through the stylesheet pipeline and emitting it hashed with its `url()` assets, rewriting the link; relative hrefs were ignored and root-absolute ones copied verbatim. Inline `<script type="module">` bodies are bundled as their own entries (Vite's html-proxy; relative imports resolve against the page) and replaced by a `src` to the hashed chunk; they were dropped, and a page with only inline scripts failed the build.
- PostCSS configuration is discovered the way postcss-load-config does: `postcss.config.{js,mjs,cjs,ts,mts,cts}`, `.postcssrc`, `.postcssrc.{json,js,mjs,cjs,ts,...}` or a `package.json` `postcss` key, searched from the app root up to the workspace root (nearest wins), with `postcss` and plugins resolved from the config's directory. A function-form config is called. In dev, PostCSS now runs on Sass output too (after Sass, as in Vite) instead of only on `.css`/Less/Stylus. Previously only three filenames in the app root were recognized.
- Worker imports in the build accept every Vite spelling in any order: `?worker&inline` bundles the worker and ships its code inside the importing chunk (started from a Blob URL, `data:` fallback), `?worker&url` and `?sharedworker&url` export the worker chunk's URL, and `new Worker(new URL("./w.ts", import.meta.url), { type: "module" })` is rewritten to a worker chunk instead of emitting the raw `.ts` as an asset. Previously only the exact `?worker`/`?sharedworker` suffix resolved.
- `new URL(\`./img/${name}.png\`, import.meta.url)` with a template literal now resolves like Vite's asset-import-meta-url: the files matching the implied glob are imported as assets and looked up at runtime, falling back to the original url when nothing matches.
- `css.preprocessorOptions` is applied in full, in dev and in the build: Sass `additionalData` (now also in the build, where it was ignored) and `loadPaths`/`includePaths`; Less and Stylus receive every option (`paths`, `javascriptEnabled`, `globalVars`, `modifyVars`, `define`, ...) plus `additionalData`. The block is read from `vite.config` too (it used to be dropped with a warning). Sass now resolves bare `@use`/`@import` specifiers like Vite's importer: through the `node_modules` directories above the importing file, via a package's `sass`/`style`/`main` entry for `@use "pkg"`, and with the `~pkg` prefix. The build's Tailwind/PostCSS and Less/Stylus sidecars now receive the source as transformed so far (plugin transforms, Sass output) instead of re-reading the file from disk, so PostCSS runs after preprocessors as in Vite.
- The JSX runtime is now configurable: `oxc.jsx.{runtime,importSource,pragma,pragmaFrag}` (what `@vitejs/plugin-react` writes from `jsxRuntime`/`jsxImportSource`) and the older `esbuild.jsx`/`jsxImportSource`/`jsxFactory`/`jsxFragment` are read from `vite.config` or `oj.config` and applied in dev and in the build, so Emotion, Theme UI and Preact apps compile against their own runtime instead of `react/jsx-runtime`. A file's `@jsx`, `@jsxRuntime`, `@jsxImportSource` and `@jsxFrag` pragma comments keep winning.
- TanStack Start's plugin bridge (the SSR loader's own plugin instances) now runs `config` and `configResolved` before any module hook, keeps plugins whose only hooks are `buildStart`, `configResolved`, `moduleParsed`, `renderChunk`, `writeBundle`, `closeBundle`, `buildEnd` or `renderStart`, and invokes each of those hooks at the matching point of the Start build (renderChunk and writeBundle for both the client and the server bundle). Hook contexts gained `this.resolve` (with Rollup's default `skipSelf`), `this.load`, `getModuleInfo`/`getModuleIds`, `this.environment.moduleGraph` and `config.createResolver`; `generateBundle` receives the real client bundle and a chunk it rewrites is written back; `resolveId`, `load` and `transform` receive Vite's `ssr` option; hooks see the configured mode (`production` for builds by default); transform `filter.code` and per-hook `order` apply. Framework plugins oj reimplements (TanStack, Vite's own, React) stay out of these hooks. (#41, #43, #44, #49, #50, #52, #63, #65-#69, #71-#73, #76, #78, #79, #83, #85, #87 by @williamhogman)
- `import.meta.glob` in the TanStack Start SSR transform: object-form `query` options are serialized like Vite, `import: "name"` selects that export in eager and lazy modes, hidden files and `node_modules` are excluded unless the pattern names them or `exhaustive: true`. (#46, #48, #56 by @williamhogman)
- `vite.config.cjs` and `vite.config.cts` are discovered, matching Vite's config file list; `publicDir: false` skips the public directory copy in Start builds. (#54, #55 by @williamhogman)
- `OJ_CACHE_DIR` relocates the cache root (`<app>/.oj-cache` by default), still versioned inside, for build systems that must keep writes out of the source tree. (#120 by @mikn)
- TanStack Start: `tsr.config.json` configures the route generator (oj's settings act as defaults under it), the generated route tree carries the framework's `declare module` footer so a checked-in tree no longer changes on every boot, an app's `package.json` `imports` entry for `#tanstack-router-entry` wins over the `src/router` convention in dev, the client bundle and the build, the `tanstack-start-injected-head-scripts:v` virtual module newer Start versions import is provided, and `oj dev --config` reaches the Start dev server. (#115-#119 by @mikn)

### Changed
- TanStack Start dev: requests reach the SSR runner over a loopback HTTP server instead of JSON lines on stdin, so request and response bodies stay binary (no lossy UTF-8 decoding, no stripped `content-length` semantics), responses stream through (the live-reload client is appended after the final chunk), and requests run concurrently: a loader that fetches the app's own route during SSR no longer deadlocks behind the single-flight runner.
- When the app has Vite installed and its config is a `vite.config`, plugins' `config`/`configResolved`/`configureServer` hooks now see the app's own resolved Vite config as their base (real `build.outDir`/`sourcemap`/`rollupOptions`, `cacheDir`, `env`, `envPrefix`, `assetsInclude()`, `css`, `ssr`, `worker`, `createResolver`, `logger`) with oj's plugin instances spliced in, instead of a synthesized config whose `build.outDir` was always `dist`. `OJ_PLUGIN_SYNTH_CONFIG=1` keeps the old synthesized config.
- String `filter.id` patterns are picomatch-style globs resolved against the root (`src/**/*.tsx`, `**/*.svg`), as in Vite's pluginFilter; a RegExp is tested on the slash-normalized id and a string `filter.code` stays a substring test. `filter.moduleType` is honored. A string id filter used to be a substring match, so globs never matched and short strings over-matched.
- A plugin `transform`, `load` or `resolveId` that throws now fails the module: in dev the request is a 500 whose message names the plugin, the file with `line:column` and the code frame (shown in the overlay); in the build the process exits non-zero. Previously the raw untransformed source was served and bundled with only a stderr line. The TanStack Start loader's plugin container surfaces the same errors instead of skipping the plugin.
- `build.target` accepts Vite's `"baseline-widely-available"` and `"modules"` presets (expanded to their browser lists) and the array form, and the default lowering is now Vite's baseline (`chrome111`, `edge111`, `firefox114`, `safari16.4`, `ios16.4`) instead of `esnext`.
- `build.emptyOutDir` follows Vite: an `outDir` outside the project root is no longer wiped unless `build.emptyOutDir: true` or `oj build --emptyOutDir` says so (a warning explains), and a `.git` directory inside `outDir` always survives.
- HMR frames use Vite's shapes: updates are one `{ type: "update", updates: [{ type: "js-update" | "css-update", path, acceptedPath, timestamp }] }` payload (stylesheet swaps included) and errors are `{ type: "error", err: { message, stack, id, loc, frame, plugin } }`, so a Vite-protocol client (the `vite-hmr` subprotocol) can consume them. `vite:beforeUpdate`/`vite:afterUpdate` receive the update payload and `vite:ws:connect`/`vite:ws:disconnect` fire. oj's own client still understands the old frames.
- Dev and build now share one asset type list, Vite's `KNOWN_ASSET_TYPES`, matched case-insensitively: `photo.JPG`, `.pdf`, `.webmanifest`, `.flac`/`.m4a`/`.aac`/`.opus`, `.vtt`, `.cur`, `.jxl` and `.txt` imports are URL assets in both, where the build used a shorter, case-sensitive list and served a camera's `.JPG` as JavaScript.

### Fixed
- The resolver now applies Vite's `development` condition in dev and `production` in the build (default conditions are `browser`/`node`, `import`, `module`, `development|production`, `default`), and a user `resolve.conditions` list is mapped like Vite: it replaces the defaults, its `development|production` placeholder becomes the active one, and `import`/`default` are always kept. The TanStack Start client and prod bundles pass the same condition. A dep whose `exports` map branches on `development` no longer resolves to one file in source and another in the pre-bundle.
- The dependency pre-bundle (optimizeDeps.include) now resolves with the dev server's own settings: `resolve.conditions`, `mainFields`, `extensions`, `preserveSymlinks` and the default entry-field order are passed to the esbuild sidecar (and folded into the pre-bundle cache key), and its tsconfig `paths` reading follows `extends` chains and every path target. Previously the sidecar used its own hardcoded `mainFields`/`conditions`, so a dual-build dep could pre-bundle a different file than a source import served.
- `import("cjs-dep")` of a CommonJS dependency is now wrapped like Vite's dynamic-import interop: the awaited namespace exposes `module.exports` as `default` and its properties as named members, instead of the raw ESM wrapper namespace.
- An import of an absolute filesystem path outside the app root (a plugin pointing at a sibling monorepo package) is now served through `/@fs` with its package allowed by the fs guard, instead of being left as a 404 url.
- A linked workspace package (a `node_modules` symlink whose real path is outside any `node_modules`) is treated as source, so plugin transforms and the source compile path apply to it as in Vite; and a plugin `load` hook with an object-form `filter.id` that names a non-prebundled dependency is now offered that dependency.
- `oj dev --ssr`: server modules now compile as dev + ssr (`import.meta.env.SSR` true, `DEV` true, `MODE` the dev mode), matching Vite's SSR transform; they used to compile with production options. The module runner's realm now exposes the web platform globals server code expects (`fetch`, `Request`/`Response`/`Headers`, streams, `crypto`, `AbortController`, `structuredClone`, `setImmediate`, `MessageChannel`, `WebSocket`, ...), shared from the host realm so `instanceof` holds.
- TanStack Start dev: a GET whose last path segment has an extension (`/robots.txt`, `/users/john.doe`, `sitemap.xml`) is served statically when a file or module owns it and otherwise reaches the app's SSR handler (server routes, dotted route params), as under `vite dev`; it used to 404 at the static server.
- TanStack Start: the config's `define` map is applied in the dev SSR loader and in both production bundles (client and server), so `__APP_VERSION__`-style globals no longer throw a ReferenceError during SSR.
- TanStack Start dev: the app's plain `.js`, `.mjs`, `.mts` and `.cts` modules go through the same SSR transform as `.ts`/`.tsx`/`.jsx` (define, `import.meta.env`, `import.meta.glob`, server functions); previously `import.meta.env.MODE` in a `.js` file threw on `undefined`. CommonJS files and dependencies keep Node's loader.
- TanStack Start dev: a `/`-prefixed import (`/favicon.txt?url`) resolves against `publicDir` after the project root in the SSR loader, as Vite does.
- Multi-page builds: a nested page's page-relative `<script src="../src/x.js">` is now rewritten to its output chunk (the `..` was never normalized, so the tag kept pointing at the source file), and split CSS is emitted once for the whole bundle instead of once per page (async chunks were getting their stylesheet injector prepended once per page). `oj preview` serves `nested/index.html` for `/nested/` and `about.html` for `/about` before falling back to the root page, matching Vite's html fallback.
- A circular import on the way from a changed module to its boundary no longer forces a full reload: like Vite, the importer already on the current path is skipped and the other importers are searched, and only reaching an entry with no accepting module reloads. Barrel-file cycles now hot-update.
- An update for a boundary the page never loaded (a lazy route, a conditional component) is ignored by the client instead of reloading the page, as Vite does.
- After the dev server restarts (a config or `.env` change) the reconnecting client reloads the page, so stale modules and defines are replaced; `oj.config.json` changes now trigger the restart too.
- Plain `@import` rules are inlined in dev-served and built CSS the way Vite's postcss-import step does: relative imports (chains included) with their `url()`s rebased to the importing file, bare specifiers (`normalize.css`, `~pkg/x`) through node_modules via the package's `style`/`main` entry, and `@import "x" print;` as `@media print { ... }`. External, `layer(...)`/`supports(...)` and unresolvable imports are kept and hoisted above the rules. Previously a bare `@import` 404ed in dev and a relative one 404ed from the build's `assets/` stylesheet.
- Assets referenced from CSS `url()` in the build now follow `build.assetsInlineLimit` (files under the limit become `data:` URLs, svg with a `#fragment` excepted) and `build.rollupOptions.output.assetFileNames`, which also names the emitted chunk stylesheets; both were hardcoded to `assets/[name]-[hash][ext]` with no inlining.
- Importing a stylesheet with `?url` or `?inline` now ships the compiled CSS in the build as in dev: `?url` emits the compiled (Sass/Less/PostCSS-processed, minified) stylesheet as a hashed `.css` asset and exports its URL, and `?inline` exports the compiled text; previously the build inlined the raw preprocessor source as an `application/octet-stream` data URI. In dev a stylesheet's `?url` points at `<file>?direct`, which serves the compiled CSS as text (so does a `<link href>` to a `.scss`), matching Vite's `?direct`.
- A plain `.svg` module import (relative, aliased like `@/assets/logo.svg`, or root-absolute) is a URL module in dev even when no plugin has a transform hook; it used to be served as raw `image/svg+xml` to the module import. Alias-resolved imports of stylesheets and assets now get the same `?import`/`?url` marks as relative ones.
- `oj build --mode <mode>` now follows Vite's NODE_ENV rule: the shell's `NODE_ENV` wins; otherwise `NODE_ENV=development` in the loaded `.env`/`.env.<mode>` makes a development build (`import.meta.env.DEV` true, `process.env.NODE_ENV` and its `global.`/`globalThis.` spellings `"development"`, `jsx-dev-runtime`); any other `.env` value is ignored with Vite's warning; otherwise the build is production. `MODE` is always the mode. This applies to the client build, the HTML `%VITE_*%` substitution, `--ssr` server and client bundles, library builds, and the TanStack Start build, which previously hardcoded production for every mode.
- `oj build --ssr` server bundles now receive the same `import.meta.env` object as the client (`VITE_*` variables, the configured `base` as `BASE_URL`, `SSR: true`) instead of five hand-written keys, and the `_oj_server_fns` bundle follows the same NODE_ENV rule as the rest of the build.
- `oj build` now evaluates `vite.config` as a build: the config is loaded with the real `command` (`build`) and `mode` (`production`, or `--mode`), so a config exported as a function (`defineConfig(({ command, mode }) => ...)`) takes its build branch, and the `build` block's `outDir`, `sourcemap`, `minify`, `cssCodeSplit`, `target` and `ssr` are honored where `oj.config` leaves them unset. Previously the config was always evaluated as `serve`/`development` and only `rollupOptions`/`assetsInlineLimit` were read from `build`.
- A stylesheet imported from JavaScript (`import "./index.css"`, the standard Tailwind layout) now hot-swaps on a source edit. The HMR client re-imports the css module wrapper on a `css-update` so the injected `<style>` is replaced in place; it used to reload the page (losing component state) because it only knew how to swap `<link>` tags.
- Editing a module that is not itself an HMR boundary (a `.ts` util imported by a component) now reaches the page. The module graph records Vite's `lastHMRTimestamp` for the invalidated chain, the re-fetched boundary's imports of those modules carry `?t=<timestamp>` (and the importer's compile key folds the stamp in, so a cached importer recompiles), so the browser loads the dependency's new version instead of its cached instance. Previously only the boundary was re-fetched and it kept importing the stale dependency.
- `oj build` fails on an unresolved import instead of emitting a partial `dist`, and the error hints at `build.rollupOptions.external`.
- HTML entry parsing accepts single-quoted, unquoted and space-padded attributes on `<script>` and `<link>` tags.
- The config's `define` map is applied to TanStack Start server modules in dev.
- The SSR loader keeps `tsconfig` path mappings inherited through `extends` with a parent `baseUrl`.
- `generateBundle` hooks honor `applyToEnvironment` consumer gates.
- A present `vite.config` that fails to load is an error (Vite's "failed to load config"), not a silent fallback to defaults; a `--config` that names a file that does not exist is an error too, and an extractor run that produced no JSON says why. (#121 by @mikn)
- The config extractor runs when reached through a symlinked path (macOS `/var` vs `/private/var`), where it used to skip its body and exit 0 as though the config were empty. (#123 by @mikn)
- TanStack Start SSR loader: JSONC trailing commas are stripped only outside strings, a UTF-8 byte-order mark in `tsconfig.json` is accepted, extensionless imports resolve `.json` and `.mts` files and indexes (Vite's `resolve.extensions`), strict-mode reserved words (`interface`, `private`, `arguments`, ...) are omitted from CommonJS facade named exports, and package `imports` targets given as fallback arrays or nested conditional objects resolve in Node's own condition order. (#45, #51, #58, #59, #60 by @williamhogman)
- Duplicate array-form `resolve.alias` entries keep first-match precedence like Vite. (#57 by @williamhogman)
- Tailwind v4's compiler and scanner are resolved through `@tailwindcss/vite` or `@tailwindcss/postcss` when a strict package layout (pnpm) nests them there instead of at the app root. (#70 by @williamhogman)
- Dev file serving: bundle-mode routes are gated to bundle mode and `server.fs.deny` matches case-insensitively.

## [0.1.11] - 2026-09-01

### Changed
- Cloudflare (TanStack Start) apps are now served by honoring the app's own `@cloudflare/vite-plugin` through Vite's dev Environment API, instead of oj driving `workerd` itself. oj's native workerd driver is removed (the `workerd`/`workerd_dev` modules, the HTTP module-fallback server, the plugin-loader sidecar, and the `wrangler` config parser). When `@cloudflare/vite-plugin` is present, the Node plugin host loads the app's installed Vite (oj takes no Vite dependency of its own), builds real `DevEnvironment`s via each environment's `dev.createEnvironment`, and lets the plugin drive Miniflare/workerd; documents and server functions both run in the worker with real vars/bindings, exactly as `vite dev` runs it. Without the plugin, SSR runs in the Node runner sidecar. Known limitations of the plugin path: worker WebSocket upgrades are not proxied (a source edit invalidating the worker's module runner, originally listed here as missing, shipped in 0.1.12).

## [0.1.10] - 2026-09-01

### Fixed
- CSS modules now resolve to the same scoped class names during SSR as on the client. The SSR loader (`loader.mjs`) reproduces oj's client class map in JavaScript (lightningcss's `[name]_[local]_[hash]` naming, including the SipHash-1-3 hash over the root-relative URL), so a `.module.css` import yields identical names on the server and the client and server-rendered markup matches the hydrated DOM. Both sides are pinned to the same literals by tests (`oj_css` and `e2e/unit/ssr-loader-css-modules.test.mjs`), so a future lightningcss naming change fails loudly rather than drifting.

## [0.1.9] - 2026-08-31

### Added
- Experimental foundation for running Cloudflare (workerd) SSR natively, with no Node, Miniflare, or Vite plugin. `oj_server::workerd` locates the `workerd` binary, generates its Cap'n Proto text config (a worker with a socket, a module-fallback address, and var/service bindings), and answers workerd's module-fallback requests with TS/JSX-stripped ESM served from oj's compiler. A bare `node_modules` import is resolved through oj's resolver (under workerd/worker export conditions) and answered with a 301 redirect to its absolute path, so a worker's whole module graph is served on demand; oj's Start module aliases (for example `tanstack-start-manifest:v`) redirect the same way to their target files, and a served module's `#`-subpath alias imports (which workerd resolves through package `imports` and never sends to the fallback) are rewritten to their target's absolute path before the module leaves the fallback. A CommonJS `node_modules` module (a `.cjs`, or a `.js` with no ES module syntax) is wrapped into ESM so workerd's ES module loader can evaluate it: its `require()` calls resolve to fallback-served paths (or a `node:` builtin under `nodejs_compat`) and `module.exports` becomes the default and named exports. The project's `wrangler.jsonc`/`.json`/`.toml` is parsed (`oj_server::wrangler`) for the worker's `compatibility_date`, `compatibility_flags` (such as `nodejs_compat`), `vars`, and service bindings, with `.dev.vars` overriding, and those feed the generated config so worker bindings resolve locally. The fallback resolves the specifier classes a real app produces: a directory import redirects to its `index` file (so the module's own relative imports resolve against the right directory), a `#`-subpath import resolves through the nearest `package.json` `imports` map, and a `.json` import is served as an ESM default export. A module the Rust tiers cannot resolve (a `virtual:` module synthesized by a Vite plugin, such as the TanStack router plugin's) is proxied to an optional plugin-loader endpoint (`workerd-plugin-loader.mjs`, which runs oj's JS plugin container) and returns native ESM, so plugin virtuals resolve without teaching Rust every plugin. On a Cloudflare project with a `workerd` binary present, `oj dev` now spawns a workerd session and the plugin loader, and routes document requests to the worker (falling back to the Node SSR runner when workerd or the loader is unavailable). `oj_server::workerd_dev::spawn` orchestrates a live session: it starts the module-fallback service, picks the worker's HTTP socket, writes the config, and spawns `workerd serve`, killing it on drop. End-to-end tests render a TypeScript route (including a `node_modules` import, and a `:`-scheme module alias) inside real workerd. Detection is gated on a Cloudflare project (`wrangler.jsonc`/`.json`/`.toml`), and `oj dev` renders a real TanStack Start Cloudflare app end-to-end. A source edit is reflected live: the file watcher evicts the changed module from an in-memory transform cache (keyed by path and modification time, self-invalidating) and reloads the running worker's isolate, so the next render serves the new output without restarting `oj dev`. The transform cache is shared between the module-fallback service and the watcher, so every render after the first reuses already-compiled modules instead of recompiling the graph.
- `oj_compiler::ssr::ssr_transform` — a Vite-compatible SSR / module-runner transform. It rewrites ES module syntax into the `__vite_ssr_*` runtime contract a Vite module runner evaluates: static imports become `await __vite_ssr_import__(...)` with references rewritten to live member access, exports become `__vite_ssr_exportName__` getters, `export *` becomes `__vite_ssr_exportAll__`, dynamic `import()` becomes `__vite_ssr_dynamic_import__`, and `import.meta` becomes `__vite_ssr_import_meta__`. Reference rewriting is scope-correct through oxc symbol resolution (shadowed locals are left alone), and live bindings are preserved. Type-only imports and exports (`import type`, `import { type X }`, `export type { … }`, `export type *`, `export { type X }`) are dropped rather than emitted as runtime imports. `ssr_transform_module` composes it with oj's existing TS/JSX strip so a `.ts`/`.tsx` module becomes runner-ready in one call. A companion `ssr-fetch-module.mjs` implements the Vite `vite:invoke`/`fetchModule` RPC responder (resolve → externalize routing → load → transform, returning a Vite `FetchResult`), and the dev server can serve runner-transformed modules over `/@ssr-module?...&runner=1`. Together these are the module-transform and fetch-module halves a Vite module runner needs, so oj can serve runner-transformed modules to an out-of-process runner (for example a workerd/Cloudflare dev runtime).

## [0.1.8] - 2026-08-29

### Fixed
- `import.meta.glob` with a TypeScript type argument is now expanded in the TanStack Start SSR path even when the type argument is nested (`import.meta.glob<Record<string, unknown>>(...)`) or an object literal (`import.meta.glob<{ default: T }>(...)`). The Start transform located the call with a regex that stopped at the first `>`, so a nested generic slipped through unrewritten and reached the SSR runtime as a real `import.meta.glob(...)` call, throwing `TypeError: (intermediate value).glob is not a function` and 500ing the route. The detector now skips a balanced, string-aware type argument before the call, matching the AST-based compiler path.

## [0.1.7] - 2026-08-29

### Added
- `server.hmr: false` disables HMR, matching Vite: file edits stop broadcasting reloads (the page holds until a manual refresh) while the dev server and plugin WebSocket keep running. Honored from both a `vite.config` and an `oj.config`.

### Fixed
- `import.meta.glob` is now expanded in `.jsx` and plain `.js`/`.mjs` modules in the TanStack Start SSR path, not only `.ts`/`.tsx`. Previously a `.jsx` route/component (or an unclaimed on-disk `.js`) that used `import.meta.glob` reached the SSR runtime untransformed and threw `TypeError: (intermediate value).glob is not a function`, 500ing the route. The dev SSR loader and both the SSR-server and client production builds now run the glob transform for the full JS/TS family.
- oj now accepts the Vite HMR WebSocket. A Vite HMR client dials the origin root with the `vite-hmr` subprotocol, but oj served its HMR socket only at `/__ws`, so that dial never upgraded and the client could not connect. oj now intercepts a `vite-hmr` upgrade on any path (matching Vite's subprotocol-based dispatch), echoes the subprotocol, and sends `{ "type": "connected" }` first, so the client connects and receives oj's existing Vite-shaped reload frames. The `vite-ping` liveness-probe subprotocol is accepted and closed immediately, as Vite does. This runs entirely on oj's native Rust WebSocket path, with no Node plugin host on the HMR path.

## [0.1.6] - 2026-08-26

### Fixed
- A module a plugin resolves via `resolveId` but does not serve from a `load` hook is now read from disk in the dev server, matching Rollup's contract (a `resolveId` result naming a real file is that module; a `load` returning nothing means read it from disk). Previously such a module 404'd — which broke a pure resolver plugin, the natural way to reach a `node_modules` tree that is not above the importer.

## [0.1.5] - 2026-08-26

### Added
- `resolve.extensions`, `resolve.mainFields`, and `resolve.preserveSymlinks` are now honored by the resolver.
- `envPrefix` accepts an array of prefixes (`string | string[]`).
- `css.preprocessorOptions.scss.additionalData` (and `sass`) injects a preamble before Sass compilation.
- `server.strictPort` is honored; by default the dev and preview servers move to the next free port when the chosen one is busy (Vite parity), and error only when `strictPort` is set.
- `optimizeDeps.esbuildOptions` (`define`, `target`, `loader`, `jsx*`, `mainFields`, `conditions`, `keepNames`, ...) is now applied to dependency pre-bundling instead of being ignored.
- `?worker&inline` emits an inline Blob worker (with a `data:` URI fallback) in bundle mode, matching Vite; in dev it serves a working module worker instead of a broken data URI.
- Plugin `handleHotUpdate` now receives the full Vite `HmrContext` (`file`, `timestamp`, `type`, `modules`, `read()`) and its returned module list is honored to narrow (or, when empty, suppress) the update, folded across plugins as in Vite.
- `build.rollupOptions`/`rolldownOptions.input` is honored (string, array, or `{ name: path }` object), so multi-page projects and projects without a root `index.html` build. HTML entries under nested directories are emitted at their root-relative path and their page-relative `<script>` sources are resolved against the page, matching Vite's `input` resolution.
- Plugins can call `this.emitFile({ type: "chunk", id })` during `buildStart` or `transform`; the chunk is bundled as a build root and `this.getFileName(referenceId)` resolves to its final hashed name in `generateBundle`. Plugin `buildStart` now runs as a rolldown build hook and receives an `options` object with `input` (the emit-chunk mechanism CRXJS-style plugins use to emit their manifest root, content scripts, and pages).
- The `generateBundle` bundle now exposes the full Rollup chunk shape (`facadeModuleId`, `moduleIds`, `modules`, `imports`, `dynamicImports`, `exports`, `isDynamicEntry`, `sourcemapFileName`; assets carry `name`/`names`), and plugins can rename an output by setting its `fileName` and delete an output with `delete bundle[key]` (used by CRXJS to rename pages and drop its manifest JS chunk).
- A plugin emitting an HTML page as a chunk (`this.emitFile({ type: "chunk", id: "...page.html" })`) now gets the Vite HTML treatment: the page's `<script type=module>` are bundled as entries, the processed page is written at its root-relative output path, and `getFileName(referenceId)` resolves to that page path (how CRXJS emits the extension's popup/options pages from its manifest).

### Added
- oj provides a `vite:css-post` plugin shim during the build. Plugins that look one up in `config.plugins` and hand it their generated CSS in `renderChunk` (UnoCSS, and similar) now have that CSS folded into the build's stylesheet output. `renderChunk` hooks also receive the full chunk shape (`modules`, `moduleIds`, `facadeModuleId`, ...) and a `NormalizedOutputOptions`-style `options` with a resolved `dir`, and a plugin-served virtual `.css` module is kept in the graph as a side-effect stub so those hooks can find it.
- The Vite build manifest is now present in the `generateBundle` bundle as a `.vite/manifest.json` asset, so plugins that read it there (for example `@crxjs`'s web-accessible-resources) can find it.

### Changed
- Plugin `transform` hooks now run on CSS and preprocessor (`.css`/`.scss`/`.less`/`.stylus`) sources during the build before oj compiles them, matching Vite where CSS is a real module in the graph. This lets directive transformers such as UnoCSS's `@apply`/`@unocss-include` resolve before Sass/lightningcss run.
- Plugin `transform` sourcemaps are now composed with oj's own transform map, so dev sourcemaps trace back to the original source through plugins that rewrite code.
- CSS imported with `?inline` returns the compiled CSS string instead of a base64 `data:` URI.
- Dev-served CSS rewrites relative `url()` and `@import` references to server-absolute paths so injected styles resolve them.

### Fixed
- `config.publicDir` is now resolved to an absolute path in `configResolved` (default `<root>/public`, Vite parity), so plugins that read it to locate assets (for example `@crxjs` loading a manifest's icons/locales) find them instead of seeing `undefined`.
- The `config`/`configResolved` hooks now see only the command-applicable plugins in `config.plugins` (serve-only plugins are excluded from a build, matching Vite), and `config.plugins` is no longer duplicated by config hooks that return their own `plugins` (the array is pinned across the config-hook merge). A plugin's `generateBundle` error is now surfaced instead of silently swallowed. Together these let `@crxjs`'s build-time manifest plugins (`transformCrxManifest`/`renderCrxManifest` fan-outs) run once and without a serve-only `crx:hmr` crashing them.
- Sass now resolves a relative dotted `@use`/`@forward`/`@import` (e.g. `@use 'colors.module.scss'`) made from inside another dotted `.module.scss` stylesheet: the nested import resolves against the imported file's real directory instead of the phantom directory grass sees a dotted basename through, so cross-directory CSS-modules stylesheets compile.
- Plugin `configResolved` now runs before `applyToEnvironment`, matching Vite; plugins that populate state in `configResolved` and read it from `applyToEnvironment` (for example `@tanstack/router-plugin`) no longer crash the plugin host, and a throwing `applyToEnvironment` keeps the plugin active instead of failing config load ([#37](https://github.com/raphamorim/oj/issues/37)).
- The `config` and `configResolved` hooks now receive `config.plugins` (the flat plugin array) and a defaulted `config.build` (`outDir`, `assetsDir`, `rollupOptions`, ...), matching Vite's resolved config. Plugins that read these in their config hooks (`@crxjs/vite-plugin` reads `config.plugins`; UnoCSS resolves `config.build.outDir`) no longer throw and get skipped.
- A module returned from a plugin `load` hook in the build is now typed from its id's extension (`.jsx`/`.tsx`/`.ts`/`.json`) instead of always JavaScript, matching how Vite derives the transform language from the id. Virtual modules like unplugin-icons' `~icons/*.jsx` are parsed as JSX again instead of failing with "JSX syntax is disabled". Build errors also now render with their source file and location rather than an opaque diagnostic dump.

### Security
- `server.fs.deny` is enforced, and oj blocks `.env`, `.env.*`, `*.crt`, `*.pem`, and `**/.git/**` by default (Vite parity).

## [0.1.4] - 2026-08-22

### Changed
- Reuse the SIMD scan finder and take a single-parse dependency fast path in `compile_factory`.

## [0.1.3] - 2026-08-22

### Changed
- The persistent cache is now opt-in via `--enable-cache` / `OJ_ENABLE_CACHE` and off by default.

## [0.1.2] - 2026-08-22

### Added
- Partial bundling: a self-contained per-package CJS-in-ESM bundler gated by `OJ_PARTIAL_BUNDLE`, with a rolldown-crate fallback (`OJ_PB_ROLLDOWN`) and a force-rolldown trigger for known-hard packages. ESM packages are bundled too.
- `optimizeDeps` configuration (`include`, `exclude`, `needsInterop`, `force`, bundler options) and `server.warmup`.

### Changed
- Compile on demand by default; `--eager` opts into the up-front graph crawl.
- Dependency pre-bundling is include-only by default; full esbuild auto-discovery is gated behind `OJ_OPTIMIZE_SCAN`.

## [0.1.1] - 2026-08-21

### Added
- Excalidraw dev benchmark harness.

### Changed
- Convert monorepo regex `resolve.alias` entries to path aliases, and keep aliased TypeScript source outside the root treated as source.
- Pass `env.config.consumer` to `applyToEnvironment`, and resolve legacy directory-entry dependency subpaths in the SSR loader.

### Fixed
- Skip `vite-plugin-checker` and survive plugins that throw asynchronously; warn once when skipping an unsupported plugin.
- Resolve dotted-basename `.module.scss` Sass imports.
- Rebuild scoping before global-define replacement.

## [0.1.0] - 2026-08-21

- First 0.1 release.
